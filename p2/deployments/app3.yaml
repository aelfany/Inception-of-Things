apiVersion: v1
kind: Service
metadata:
  name: app-three-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5678
  selector:
    app: app-three
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-three
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-three
  template:
    metadata:
      labels:
        app: app-three
    spec:
      containers:
        - name: app-three
          image: hashicorp/http-echo
          args:
            - "-text=Hello From App3 by Kacimo!"
          ports:
            - containerPort: 5678